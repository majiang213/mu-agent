interface:
  display_name: "Mu Agent"
  short_description: "Repo-specific patterns and workflows for mu-agent"
  default_prompt: "Use the mu-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true